name: Benchmark

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: '*'
          cache: 'npm'
          check-latest: true

      - name: Install dependencies
        run: npm ci

      - name: Run benchmarks
        run: npm run benchmark

      - name: Run Delta
        uses: netlify/delta-action@1c4b751be6fe509418f2cdcc141dd364953c6546
        with:
          title: '⏱ Benchmark results'
          token: ${{ secrets.GITHUB_TOKEN }}
